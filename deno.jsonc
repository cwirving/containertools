{
  "tasks": {
    "test": "deno test --allow-read --allow-write tests/*_test.ts",
    "lint": "deno lint src/*.ts tests/*.ts",
    "check": "deno check src/*.ts tests/*.ts",
    "fmt": "deno fmt src/*.ts tests/*.ts"
  }
}
